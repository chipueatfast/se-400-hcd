/// <reference types="react" />
import type { ActionBarProps } from './ActionBar';
export declare const Stack: ({ headlineLeft, headlineRight, price, primary, secondary, actions, back, onCloseModal, shouldRenderFooter, children, }: ActionBarProps) => JSX.Element;
