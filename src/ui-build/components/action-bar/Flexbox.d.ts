/// <reference types="react" />
import type { ActionBarProps } from './ActionBar';
export declare const Flexbox: ({ headlineLeft, primary, secondary, back, actions, onCloseModal, shouldRenderFooter, children, }: ActionBarProps) => JSX.Element;
